<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Matches index.html's own root bg color variable (#0a0d12) exactly, so the
         native window background is indistinguishable from the WebView content
         during the brief pre-paint window. -->
    <color name="nova_bg">#0a0d12</color>
    <color name="nova_accent">#6ea8ff</color>
</resources>
