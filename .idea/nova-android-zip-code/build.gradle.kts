// Top-level build file — declares plugin versions once, applied per-module below.
// Versions chosen as of early 2026: AGP 8.5.x / Kotlin 2.0.x are the current stable
// line for a project targeting compileSdk 34 (Android 14). If Android Studio's
// built-in upgrade assistant suggests newer versions when you open this project,
// that's fine to accept — nothing here depends on an exact patch version.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "2.0.20" apply false
}
