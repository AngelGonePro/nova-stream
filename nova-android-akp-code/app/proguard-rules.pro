# Minification is off (see app/build.gradle.kts — isMinifyEnabled = false) for
# this first pass, specifically so nothing here needs debugging obfuscated
# stack traces on top of everything else that's genuinely new and untested.
# Revisit turning this on once the app is confirmed working end-to-end.
